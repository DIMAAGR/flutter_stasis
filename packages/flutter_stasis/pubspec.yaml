name: flutter_stasis
description: "State management for Flutter built around explicit lifecycle, standardised async execution, and ephemeral UI events."
version: 0.3.1
homepage: https://github.com/DIMAAGR/flutter_stasis
repository: https://github.com/DIMAAGR/flutter_stasis

topics:
  - state-management
  - mvvm
  - flutter
  - architecture

environment:
  sdk: ^3.10.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_stasis_core: ^0.3.1


dev_dependencies:
  flutter_lints: ^6.0.0
