## 0.1.0

- Initial `flutter_stasis_secure` package.
- Added `SafeDataSecureAdapter` contract.
- Added `FakeSafeDataSecureAdapter` for tests and local development.
- Added `SafeDataSecureBinding<T>` for explicit persist, restore, and delete flows.
